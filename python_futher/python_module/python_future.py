# python package '__future__'
from __future__ import division

print(5/3)
print(divmod(5, 3))  # 结果：商+余数

# use the built-in dir function to list the identifiers that a module defines.
print(dir(division))
